<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel='stylesheet' href='/components/bootstrap/dist/css/bootstrap.css' />
    <link rel='stylesheet' href='/components/font-awesome/css/font-awesome.css' />
    <style>
	    html, body
		{
			background: rgba(39,54,72,1);
			margin :0;
			

		}

    	.rankcontain{
			width: 100%;
			overflow: auto;
    	}
		.rank{
			width: 100%;
			height: auto;
			max-height: 20%;
			display: inline-block;
			margin-bottom: 5px;
			padding: 0;
			overflow: hidden;
			background: rgba(251,241,241,0.9);
		}
		.photo{
			width: 30%;
			float: left;
		}
		.photo img{
			padding: 10px;
			width: 100%;
			height: 100%;

		}
		.rank .info{
			float: left;
			width: 45%;
			padding: 10px;
			
		}
		.rank .info .name{

		}
		.rank .info .score{

		}
		.rank .add{
			position: absolute;
			width: 25%;
			overflow: hidden;
			top: 5px;
			bottom: 15px;
			right:15px;
			background: #E6A1AD;
			display: inline-block;
		}

		.rank .add .addbut{
			height: 100%;
  display: -webkit-flex;
  display:         flex;
  -webkit-align-items: center;
          align-items: center;
  -webkit-justify-content: center;
          justify-content: center;
          color: rgba(221,213,215,1);
		}
		.rank .add .addcover{
			position: absolute;
			top:0px;
			width:100%;
			height: 100%;
			background: rgba(221,213,215,1);
		}
    </style>
  </head>
  <body ng-app="myApp" ng-controller="indexCtrl">

  	<div style="font-size: 5em; padding: 10px; width: 100%; text-align: center; font-weight: bold;color: white;">建 榜</div>
	<div class="rankcontain">
		<div class="col-lg-4 col-lg-offset-4 col-xs-12" ng-repeat="user in users">
			<div class="rank">
				<div class="photo">
					<img src="/images/{{user.name}}.png" alt="">
				</div>
				<div class="info">
					<div class="name"><h4>{{user.name}}</h4></div>
					<div class="score"><h4>{{user.basic}} + {{user.extra}}</h4></div>
					
				</div>
				<div class="add">
					<div class="addcover" ng-hide="user.click" ng-click="clickAdd(user)">
						
					</div>
					<div class="addbut" ng-show="user.click" ng-click="clickAddConfirm(user)">
						<i class="fa fa-plus fa-3x"></i>
					</div>
					
					
				</div>
				
			</div>	
			
		</div>
	</div>

    <script src="/components/jquery/dist/jquery.min.js"></script>
    <script src="/components/bootstrap/dist/js/bootstrap.min.js"></script>
    <script src="/components/angular/angular.js"></script>
    <script src="/javascripts/app.js"></script>
  </body>
</html>
